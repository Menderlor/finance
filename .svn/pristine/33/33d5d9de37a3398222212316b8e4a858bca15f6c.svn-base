package com.cedarhd.control;

import android.content.Context;
import android.util.AttributeSet;
import android.view.LayoutInflater;
import android.view.View;
import android.widget.LinearLayout;

import com.cedarhd.R;
import com.cedarhd.utils.LogUtils;

/**
 * 自定义头像控件
 * 
 * @author bohr
 * 
 */
public class AvartarView extends LinearLayout {
	public AvartarView(Context context) {
		super(context);
		LogUtils.i("context", "context1");
		LayoutInflater.from(context).inflate(R.layout.control_avartar_layout,
				this, true);
	}

	/**
	 * 头像控件构造函数
	 * 
	 * @param context
	 * @param attrs
	 */
	public AvartarView(Context context, AttributeSet attrs) {
		super(context, attrs);
		LogUtils.i("context", "context2");
		LayoutInflater.from(context).inflate(R.layout.control_avartar_layout,
				this, true);
	}

	/**
	 * 头像构造
	 * 
	 * @param context
	 * @param attrs
	 * @param isShowName
	 *            是否显示姓名
	 * @param width
	 *            头像宽度
	 * @param height
	 *            头像高度
	 */
	public AvartarView(Context context, AttributeSet attrs, boolean isShowName,
			float width, float height) {
		super(context, attrs);
		LogUtils.i("context", "context2");

		View view = LayoutInflater.from(context).inflate(
				R.layout.control_avartar_layout, this, true);
	}
}
