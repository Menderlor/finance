<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/style_menu_grid_item_rl" >

    <ImageView
        android:id="@+id/ivNewcompanyspace"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/add"
        android:visibility="gone" />
    
    <ImageView
        android:id="@+id/ivListcompanyspace"
        style="@style/style_menu_grid_item_iv"
        android:src="@drawable/kongjian" />
    
    <TextView
        android:id="@+id/tvNewcompanyspaceCount"
        style="@style/style_menu_badge_textview"
        android:visibility="gone"/>
    <TextView
        style="@style/style_menu_grid_item_text"
        android:layout_below="@+id/ivListcompanyspace"
        android:text="空间" />
</RelativeLayout>